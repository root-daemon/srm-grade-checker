<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="A SRM CGPA calculator and Grade calculator to plan about your academic comeback (which will never happen)."
    />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <!-- Option 1: Include in HTML -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css"
    />

    <title>GradeX</title>
  </head>
  <body>
    <slot />
  </body>
</html>
<style is:global>
  @import url("https://fonts.googleapis.com/css2?family=Manrope:wght@500;700&display=swap");
  :root {
    --bg: #0a0b0a;
    --border: #22291f;
    --brand: #d8ffc6;
    --text-color: #f5f5f5;
    --card: #0b0d0b;
    --incard: #0e100e;
    --input: #191b19;
    --tip: #3e3f3e;
    --sno: #a4a4a4;
    --red: #ffa3a3;
  }
  html {
    text-rendering: geometricPrecision !important;
    overflow-x: hidden;
    color: var(--text-color);
    font-family: "Manrope";
    background: var(--bg);
    font-weight: 600;
    scroll-behavior: smooth;
    -webkit-transition: 0.2s cubic-bezier(0.21, 0.67, 0.31, 0.92);
    -o-transition: 0.2s cubic-bezier(0.21, 0.67, 0.31, 0.92);
    transition: 0.2s cubic-bezier(0.21, 0.67, 0.31, 0.92);
  }
  h1,
  h2,
  h3 {
    font-weight: 800 !important;
  }
  * {
    font-family: "Manrope";
    font-weight: 600;
  }

  ::-webkit-scrollbar {
    width: 16px;
    -webkit-transition: 0.3s cubic-bezier(0.21, 0.67, 0.31, 0.92) !important;
    transition: 0.3s cubic-bezier(0.21, 0.67, 0.31, 0.92) !important;
  }

  ::-webkit-scrollbar-track {
    background-color: var(--incard);
  }

  ::-webkit-scrollbar-track:hover {
    background-color: var(--incard);
  }

  /* scrollbar itself */
  ::-webkit-scrollbar-thumb {
    background: var(--brand);
    border-radius: 16px;
    border: 6px solid var(--incard);
  }

  ::-webkit-scrollbar-thumb:hover {
    border: 5px solid var(--incard);
  }

  ::-webkit-scrollbar-corner {
    background: var(--brand);
  }

  ::-webkit-scrollbar-button {
    display: none;
  }
</style>
